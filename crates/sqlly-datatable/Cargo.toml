[package]
name = "sqlly-datatable"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "A configurable data grid component for GPUI"

[dependencies]
gpui = { version = "0.2" }
smol = "2.0"
